superdomain: ${threescale.superdomain}
tenant_name: "3scale",
admin_endpoint: ${tenant_name}"-admin."${threescale.superdomain}
dev_endpoint: ${tenant_name}"."${threescale.superdomain}
staging_domain: ${tenant_name}"-apicast-staging."${superdomain}
production_domain: ${tenant_name}"-apicast-production."${superdomain}
